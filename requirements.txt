# Elephant Lab v3 Python dependencies
# Update 2026-04-20: Sprint 1 감사 반영 (stable-baselines3, scipy, pyarrow 추가)

# 데이터/수치
# Codex 권고 9 (2026-05-09): numpy ABI mismatch 방지 위해 numpy<2.2는 유지.
# 2026-05-12 canonical env 검증값 반영: pandas 2.3.3 / pyarrow 24.0.0 /
# scipy 1.17.1 / scikit-learn 1.8.0 / stable-baselines3 2.8.0 /
# gymnasium 1.2.3 / torch 2.11.0.
numpy>=1.24,<2.2
pandas>=2.0,<2.4
scipy>=1.10,<1.18        # metrics.py _pct_rank (S1-0 Batch B)
pyarrow>=10.0,<25.0      # parquet I/O (backfill.py, dataset_builder.py)

# 금융 데이터
pykrx>=1.0
yfinance>=0.2.0

# ML
scikit-learn>=1.3,<1.9   # ABI compat with numpy<2.2
lightgbm>=4.0,<4.7       # ranking_loss.py, lgbm_trainer.py (S1-0 Batch B/C)
stable-baselines3>=2.0,<2.9   # ppo_allocator.py policy 로드 (S3-7 재학습 시 활성화)
gymnasium>=0.29,<1.3      # stable-baselines3 backend + PPO retrainer tests
torch>=2.0,<2.12          # stable-baselines3 의존

# 설정/검증
PyYAML>=6.0
jsonschema>=4.17
python-dotenv>=1.0

# HTTP
requests>=2.28

# gRPC (AI-BE bridge)
grpcio>=1.60,<2.0
grpcio-tools>=1.60,<2.0

# LLM (Sprint 2+ Cold Path / Mode B)
openai>=1.0

# 시계열 피처 추출
tsfresh>=0.20  # S2-6 News filter TSFresh 30분 통계 → 자연어 변환 (text_pack_builder)

# NLP (S4-1 DualSourceScorer FinBERT 한국어 sentiment)
# snunlp/KR-FinBert-SC 또는 ProsusAI/finbert 모델 사용. 미설치 시 sentiment_dict fallback.
# 2026-05-13 추가: dual_source 80일 archive deploy-quality 점수 산출용.
transformers>=5.0,<6.0

# 테스트
pytest>=7.0
