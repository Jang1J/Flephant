# Elephant Lab v3 Python dependencies
# Update 2026-04-20: Sprint 1 감사 반영 (stable-baselines3, scipy, pyarrow 추가)

# 데이터/수치
numpy>=1.24
pandas>=2.0
scipy>=1.10              # metrics.py _pct_rank (S1-0 Batch B)
pyarrow>=10.0            # parquet I/O (backfill.py, dataset_builder.py)

# 금융 데이터
pykrx>=1.0
yfinance>=0.2.0

# ML
scikit-learn>=1.3
lightgbm>=4.0            # ranking_loss.py, lgbm_trainer.py (S1-0 Batch B/C)
stable-baselines3>=2.0   # ppo_allocator.py policy 로드 (S3-7 재학습 시 활성화)
torch>=2.0               # stable-baselines3 의존

# 설정/검증
PyYAML>=6.0
jsonschema>=4.17
python-dotenv>=1.0

# HTTP
requests>=2.28

# LLM (Sprint 2+ Cold Path / Mode B)
openai>=1.0

# 시계열 피처 추출
tsfresh>=0.20  # S2-6 News filter TSFresh 30분 통계 → 자연어 변환 (text_pack_builder)

# 테스트
pytest>=7.0
